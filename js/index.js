// Iteration 1: Names and Input
let hacker1 = "Natasha";
console.log("The driver's name is ${hacker1}");

let hacker2 = "Bob";
console.log("The navigator's name is ${hacker2}");


// Iteration 2: Conditionals
if (hacker1.length > hacker2.length) {
  console.log("The driver has the longest name, it has ${hacker1.length} characters");
}
else if (hacker1.length < hacker2.length) {
  console.log("It seems that the navigator has the longest name, it has XX characters.");
}
else if (hacker1.length = hacker2.length) {
  console.log("Wow, you both have equally long names, XX characters!");
}

// Iteration 3: Loops
//3.1 Print all the characters of the driver's name, separated by a space and in capitals i.e. "J O H N"
 
 let newDriverName = ""; //this is the original driver's name, but with the characters separated by spaces and in capital letters, so javascript will have to recognize it as a technically new name (but it's really just the old name with fancy commands, but we have to define the name in a new way because of this, hence "newDriverName")
 let driver = hacker1 //need to add this b/c 

 for (let i=0; i < driver.length; i++) {
   if ()
 }

 //console.log(newDriversName)

//I know I have to use splice and charAt here, but I am lost at this moment and have spent hours on this already as I was confused about the instructions for the LAB | JS Basic Algorithms