// Iteration 1: Names and Input
//
// Iteration 2: Conditionals


// Iteration 3: Loops



const driver = prompt("What's your name?");
const navigator = prompt("What's your name?");

console.log(`The drivers name is ${driver}`);
console.log(`The navigators name is ${navigator}`);



if (driver.lenght> navigator.lenght) {
    console.log (`The driver has the longest name, it has ${driver.lenght} characters `)
} else if (driver.lenght<navigator.lenght){
    console.log (`It seems that the navigator has the longest name, it has ${navigator.lenght} characters.`)}
    else {
    console.log (`Wow, you both have equally long names, ${driver.lenght} characters!`)
    }

 console.log (driver.toUpperCase().split("").join(" "));
 console.log (navigator.split("").reverse().join(""));

 //'A' < 'B'
 //'driver' < 'navigator'

 //const palindrome = "A man, a plan, a canal, Panama!"
 //const regex = /[Â-Za-z]/

 //const plainWord = palindrome.replace(regex, "").toLowerCase()

 //console.log("ESTO HACE EL REGEX ==>", plainWord
 


    


